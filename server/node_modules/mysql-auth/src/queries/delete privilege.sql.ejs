<%

if(args.length === 0) {
	throw new Error("Argument 1 is required to <deletePrivilege>");
}
if(typeof args[0] !== "object") {
	throw new Error("Argument 1 must be an object to <deletePrivilege>");
}

const wherePrivilegeSQL = utils.whereToSQL(args[0]);

%>

DELETE FROM $auth$privilege
	WHERE <%-wherePrivilegeSQL%>;