<%

if(typeof args !== "object") {
	throw new Error("Argument 1 must be an object to <findUser>");
}
if(Object.keys(args).length === 0) {
	throw new Error("Argument 1 must have 1 property minimum to <findUser>");
}

const whereUserSQL = utils.whereToSQL(args[0]);

%>

SELECT * FROM $auth$user WHERE <%- whereUserSQL %>;